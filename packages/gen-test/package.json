{
  "name": "gen-test",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bun": "^1.2.23"
  },
  "devDependencies": {
    "@types/bun": "^1.2.23"
  },
  "scripts": {
    "clean": "rm -rf src/rapidhash && rm -rf generated/ && mkdir -p generated",
    "generate": "bun run src/index.ts",
    "cp": "rm -rf ../rapidhash-js/test/test-vectors && mkdir -p ../rapidhash-js/test/test-vectors && cp -R generated/ ../rapidhash-js/test/test-vectors"
  },
  "private": true
}
