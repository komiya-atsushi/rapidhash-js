{
  "name": "gen-test",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bun": "^1.2.23"
  },
  "scripts": {
    "clean": "rm -rf src/rapidhash && rm -rf generated/ && mkdir -p generated",
    "pull:rapidhash": "git -C src/rapidhash pull origin master || git clone https://github.com/Nicoshev/rapidhash.git src/rapidhash",
    "generate": "bun run pull:rapidhash && bun run src/index.ts",
    "cp": "cp -R generated/ ../rapidhash-js/test"
  },
  "private": true
}
